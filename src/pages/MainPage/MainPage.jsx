import React from 'react';
import s from './MainPage.module.scss';

const MainPage = () => {
  return (
    <main className={s.mainPage}>
      <h1 className={s.title}>Main</h1>
    </main>
  );
};

export default MainPage;
